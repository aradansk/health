---
phase: 01.0-fasten-ingest-api-spike
verified: 2026-05-19T15:00:00Z
status: passed
score: 5/5 must-haves verified
overrides_applied: 0
re_verification: false
---

# Phase 1.0: Fasten Ingest API Spike — Verification Report

**Phase Goal:** Resolve the highest-risk MEDIUM-confidence assumption (Fasten's undocumented FHIR Bundle POST endpoint) before plan-lock; produce a working programmatic ingest path or a documented fallback (volume-mount JSON drop / direct SQLite write / UI scrape). Audit Fasten stdout logs for PII leakage as a paired deliverable.
**Verified:** 2026-05-19T15:00:00Z
**Status:** PASSED
**Re-verification:** No — initial verification

---

## Goal Achievement

### Observable Truths (ROADMAP Success Criteria)

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | A Python script POSTs a 1-resource FHIR Bundle to a running Fasten container and the resource appears (retrievable via API) within seconds — OR a documented fallback path with concrete commands | VERIFIED | `docs/fasten-admin.md` §6: "spike_ingest.py exited 0 against the live throwaway container"; step [E] "sentinel ZZZ-PII-CANARY-0001 retrievable"; EXIT=0 recorded in §6 verbatim output block. SUMMARY confirms exit 0 empirically. |
| 2 | Fasten stdout logs reviewed against PII Tier 1 baseline; findings noted with mitigation (LOG_LEVEL / redaction sidecar / restricted mount) | VERIFIED | `docs/fasten-admin.md` §4.1: audit table shows 0 canary occurrences at INFO and WARN. §4.1.b (CR-01 fix, commit 1ef0775) adds the SECRET-in-log finding with a blocking acceptance gate for Phase 1.4. Chosen mitigations listed at §4.1. |
| 3 | Chosen Fasten image digest recorded with ingest endpoint, auth pattern, and response semantics | VERIFIED | `docs/fasten-admin.md` §2 table: pinned `sha256:3f0192ac…` and live index `sha256:d208351137…` both recorded. §1 documents full endpoint+auth+envelope. INFRA-06 digest drift handling documented without silent re-pin. |
| 4 | `docs/fasten-admin.md` checked into the repo as source of truth for downstream ETL phases | VERIFIED | Commit 787c425 created the file; 1ef0775 applied CR-01/WR fixes. `git show --stat` confirms both commits modify `docs/fasten-admin.md`. File exists at `docs/fasten-admin.md` (397 lines). |
| 5 | Spike completes within 2 working days; if blocked, escalation note documents the blocker + chosen fallback (no silent overrun) | VERIFIED | SUMMARY states "~11 min, far inside the 2-day box." `docs/fasten-admin.md` §6 heading "Escalation / Blocked Outcome" present with "PRIMARY PATH CONFIRMED" and verbatim exit-0 evidence block. |

**Score:** 5/5 truths verified

---

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `docs/fasten-admin.md` | Durable ingest contract: endpoint, auth, response envelope, pinned digest, jwt.issuer.key note, LOG_LEVEL decision, fallback ranking, escalation section | VERIFIED | 397 lines. All required terms confirmed present: `/api/secure/source/manual`, `/api/secure/access/token`, `sha256:3f0192ac77dda7fd0e25175f7383a426f6d5ee11be7981b8753c10f6b7447d91`, `jwt.issuer.key`, `LOG_LEVEL`, `Escalation`. Fallback ranking table (rank 0–4) present at §5. |
| `spike/compose.spike.yaml` | Digest-pinned single-service Fasten, FASTEN_JWT_ISSUER_KEY override, throwaway values | VERIFIED | Contains exact digest `sha256:3f0192ac77dda7fd0e25175f7383a426f6d5ee11be7981b8753c10f6b7447d91`. `FASTEN_JWT_ISSUER_KEY=spike-throwaway-key-not-prod` overrides the published insecure default. Bind-mounts `config.spike.yaml` (OQ2 resolution). |
| `spike/spike_ingest.py` | Assertive spike script: signup/signin, access token, FHIR Bundle build+validate, POST manual, verify, idempotency probe, PII grep helper; non-zero exit on failure | VERIFIED | 245 lines. References all 4 required endpoints. Uses `files={"file": ...}` multipart form field. Non-zero exit via `fail()` on any assertion. `--twice` and `--pii-grep` modes present. fhir.resources pydantic validation with fallback. |
| `spike/sample_bundle.json` | 1-Observation collection Bundle, synthetic ZZZ-PII-CANARY-0001 only | VERIFIED | Valid JSON, `"type": "collection"`, Patient + Observation entries, sentinel `ZZZ-PII-CANARY-0001` in Patient name fields and Observation `valueString` and note. No real health values. |
| `spike/config.spike.yaml` | Throwaway encryption key + jwt.issuer.key, HTTPS disabled (OQ2 working form) | VERIFIED | Provides `database.encryption.key` (exits STANDBY mode), `jwt.issuer.key` override, `web.https.enabled: false`. Clearly marked THROWAWAY. This was the in-line OQ2 resolution artifact created in commit 1feb50d. |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `spike/spike_ingest.py` | `POST /api/secure/source/manual` | httpx multipart, field name `file` | VERIFIED | Line 143: `files={"file": ("bundle.json", bundle_bytes, "application/json")}` |
| `spike/spike_ingest.py` | `POST /api/secure/access/token` | Bearer session JWT exchange | VERIFIED | Lines 125-136: `client.post(f"{BASE}/api/secure/access/token", headers={"Authorization": f"Bearer {session_jwt}"}, json={"name":"etl-spike","expiration":0})` |
| `docs/fasten-admin.md` | Phase 1.4 ETL fhir_client design | documented endpoint + auth + response semantics + digest + fallback ranking | VERIFIED | §0 TL;DR states 3 calls for Phase 1.4. §1.4/1.5 resolve OQ1/OQ2. §2 records digest + drift decision. §3 mandatory Vaultwarden secrets for 1.1/1.4. §6 PRIMARY PATH CONFIRMED. |
| `docs/fasten-admin.md` | INFRA-06 digest-drift finding | live vs pinned digest table + Phase 1.4 re-pin decision | VERIFIED | §2 table records both `sha256:3f0192ac…` (amd64 manifest) and `sha256:d208351137…` (live index). §2.1 instructs Phase 1.4 to consciously choose pin and re-verify. No silent re-pin. |

### Data-Flow Trace (Level 4)

Not applicable. This phase produces documentation and throwaway spike scripts, not components rendering dynamic data from a live data source. Step 7b behavioral checks also not applicable (the throwaway Fasten container was torn down by design as a success criterion; the spike is not a persistent runnable service).

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| spike_ingest.py exits 0 against live container | `python spike_ingest.py` | SUMMARY: exit 0; §6 of fasten-admin.md records verbatim step-by-step 200 responses | DOCUMENTED (container torn down by design; verified via SUMMARY + fasten-admin.md §6 evidence block) |
| docs/fasten-admin.md contains all required terms | Python one-liner from Task 3 verify | All 6 required strings present (confirmed by Grep above) | PASS |
| No Fasten source vendored (GPL-3.0 firewall) | Grep for Fasten source tree in repo | Only RESEARCH.md references (2 occurrences in plan/research docs) — no source tree | PASS |

Step 7b server-based checks are SKIPPED: the throwaway container was torn down as part of phase completion (success criterion, not a gap). Behavioral evidence is recorded in `docs/fasten-admin.md` §6.

### Probe Execution

No `probe-*.sh` scripts declared or conventionally expected for a documentation spike. SKIPPED.

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|------------|-------------|--------|---------|
| INFRA-06 | 01.0-01-PLAN.md | Image pinning by digest — Fasten `:main@sha256:<digest>` | SATISFIED (partial — spike scope) | `docs/fasten-admin.md` §2 records the pinned digest, documents drift finding, instructs Phase 1.4 on re-pin choice. Full INFRA-06 production pinning (compose.yaml) is Phase 1.1; spike contributes the FHIR-layer digest record. ROADMAP confirms INFRA-06 is shared 1.0+1.1. |
| DATA-02 | 01.0-01-PLAN.md | Fasten OnPrem digest-pinned, SQLite-only; SQLite file on encrypted host volume | SATISFIED (partial — audit deliverable scope) | `docs/fasten-admin.md` §4.1 provides the PII-log audit at INFO and WARN (canary 0 occurrences). §4.1.b adds the SECRET-in-log finding. Full DATA-02 (production Fasten container on encrypted volume) is Phase 1.4. ROADMAP confirms DATA-02 is shared 1.0+1.4. REQUIREMENTS.md traceability row maps DATA-02 to Phase 1.4 only, but ROADMAP coverage note (line 232) explicitly states `DATA-02 → 1.0+1.4`. No gap — the spike delivers the scoped portion. |

**Note on DATA-02 traceability discrepancy:** REQUIREMENTS.md traceability table lists DATA-02 under Phase 1.4 only, while the ROADMAP coverage summary explicitly states `02 → 1.0+1.4`. The PLAN frontmatter claims `DATA-02` for Phase 1.0 as the PII-log audit deliverable. These are consistent in intent; the REQUIREMENTS.md table is a per-phase primary assignment, while the ROADMAP notes shared contributions. Not a blocking gap — the spike's scoped contribution is delivered.

### Code Review Status

A full code review ran (01.0-REVIEW.md, 2026-05-19) against `docs/fasten-admin.md`:

- **1 Critical (CR-01):** SQLCipher encryption key logged in cleartext to docker logs; §4.1 "PII-clean" audit never checked for it. **FIXED in commit 1ef0775:** §3 now explicitly flags the SECRET-bearing nature of boot logs; §4.1 qualified to "ingest data path only"; §4.1.b added with SECRET LEAK verdict + blocking acceptance gate for Phase 1.4.
- **3 Warnings (WR-01/02/03):** Token-mint payload inconsistency; upstream default jwt.issuer.key reproduced without guard; idempotency blast radius undocumented. **All FIXED in commit 1ef0775:** canonical `"name":"etl"` with free-form-label note; NEVER-copy guard on default key value; O(n-runs) blast radius documented in §4.2.
- **3 Info (IN-01/02/03):** Non-expiring token revocation path; DEBUG/TRACE levels unaudited; TLS precondition implicit. **Documented as non-blocking future improvements.** Not treated as phase gaps per spike verification notes.

### Anti-Patterns Found

| File | Pattern | Severity | Impact |
|------|---------|----------|--------|
| `spike/config.spike.yaml` line 33 | `key: 'spike-throwaway-32byte-encryption-key-not-prod'` | INFO | Literal secret-shaped value in file — INTENTIONAL (throwaway spike artifact, clearly labeled THROWAWAY, not committed as production config, documented as non-production in 3 comment lines) |
| `spike/spike_ingest.py` line 52 | `ADMIN_PASS = os.environ.get("SPIKE_ADMIN_PASS", "spike-throwaway-pass-not-prod")` | INFO | Hardcoded fallback credential — INTENTIONAL (throwaway spike, env-overridable, clearly labeled not-prod) |

No TBD / FIXME / XXX debt markers found in phase-modified files. No stub implementations in the durable deliverable (`docs/fasten-admin.md` is a complete, empirically-backed contract). Throwaway-labeled secrets in spike/ files are by design and properly scoped.

### Human Verification Required

None. This phase produced a documentation deliverable (`docs/fasten-admin.md`) verified against observable codebase artifacts (file exists, content confirmed, commits confirmed). The empirical spike results (exit code 0, canary retrievable) are recorded verbatim in `docs/fasten-admin.md` §6 and cannot be re-run because the throwaway container was torn down as a success criterion. The recording of empirical evidence in the durable deliverable is the designed verification mechanism for a spike.

---

## Gaps Summary

No gaps. All 5 ROADMAP success criteria are verified against the codebase:

1. Programmatic ingest path confirmed and documented (spike exit 0, canary retrievable, evidence in fasten-admin.md §6).
2. PII-log audit completed at INFO and WARN with findings and mitigation; SECRET-in-log finding added (CR-01 fix).
3. Digest recorded with full contract; drift documented without silent re-pin.
4. `docs/fasten-admin.md` committed to repo (commits 787c425, 1ef0775).
5. Completed in ~11 min; escalation section present with PRIMARY PATH CONFIRMED.

The code review Critical and all Warnings were resolved in commit 1ef0775 before this verification ran. The 3 Info items are documented non-blocking improvements and are not treated as phase gaps.

---

_Verified: 2026-05-19T15:00:00Z_
_Verifier: Claude (gsd-verifier)_
