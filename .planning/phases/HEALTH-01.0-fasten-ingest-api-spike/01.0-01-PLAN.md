---
phase: 01.0-fasten-ingest-api-spike
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/HEALTH-01.0-fasten-ingest-api-spike/spike/compose.spike.yaml
  - .planning/phases/HEALTH-01.0-fasten-ingest-api-spike/spike/spike_ingest.py
  - .planning/phases/HEALTH-01.0-fasten-ingest-api-spike/spike/sample_bundle.json
  - docs/fasten-admin.md
autonomous: true
requirements: [INFRA-06, DATA-02]
user_setup: []

must_haves:
  truths:
    - "A Python script POSTs a 1-resource FHIR Bundle to a running Fasten container and the synthetic canary resource is retrievable via the Fasten API within seconds (success criterion 1)"
    - "Fasten stdout logs are audited against the PII Tier 1 baseline; canary presence/absence at INFO and WARN is recorded with a chosen mitigation (success criterion 2)"
    - "The multi-arch OCI index digest sha256:3f0192ac77dda7fd0e25175f7383a426f6d5ee11be7981b8753c10f6b7447d91 is recorded with the ingest endpoint, auth pattern, and response semantics (success criterion 3)"
    - "docs/fasten-admin.md is checked into the repo as the source of truth for downstream ETL phases 1.4+ (success criterion 4)"
    - "If the spike is blocked, an escalation note inside docs/fasten-admin.md documents the blocker and the chosen fallback rank — no silent overrun (success criterion 5)"
  artifacts:
    - path: ".planning/phases/HEALTH-01.0-fasten-ingest-api-spike/spike/compose.spike.yaml"
      provides: "Throwaway digest-pinned single-service Fasten with FASTEN_JWT_ISSUER_KEY override"
      contains: "sha256:3f0192ac77dda7fd0e25175f7383a426f6d5ee11be7981b8753c10f6b7447d91"
    - path: ".planning/phases/HEALTH-01.0-fasten-ingest-api-spike/spike/spike_ingest.py"
      provides: "Assertive spike script: signin -> mint access token -> build+validate Bundle -> POST manual -> verify -> re-POST probe -> PII grep"
      min_lines: 60
    - path: ".planning/phases/HEALTH-01.0-fasten-ingest-api-spike/spike/sample_bundle.json"
      provides: "1-Observation FHIR Bundle, synthetic canary values only"
      contains: "ZZZ-PII-CANARY-0001"
    - path: "docs/fasten-admin.md"
      provides: "Durable ingest contract: endpoint, auth, response envelope, pinned digest, jwt.issuer.key note, LOG_LEVEL decision, fallback ranking, escalation section"
      contains: "/api/secure/source/manual"
  key_links:
    - from: "spike/spike_ingest.py"
      to: "POST /api/secure/source/manual"
      via: "httpx multipart upload, field name file, Authorization Bearer access token"
      pattern: "api/secure/source/manual"
    - from: "spike/spike_ingest.py"
      to: "POST /api/secure/access/token"
      via: "exchange 1h session JWT for long-lived access JWT"
      pattern: "api/secure/access/token"
    - from: "docs/fasten-admin.md"
      to: "Phase 1.4 ETL fhir_client design"
      via: "documented endpoint + auth + response semantics + digest + fallback ranking"
      pattern: "sha256:3f0192ac"
---

<objective>
Empirically confirm Fasten OnPrem's now-known programmatic FHIR Bundle ingest contract, run the paired PII-log audit, and lock the result in `docs/fasten-admin.md` as the source of truth for downstream ETL phases (1.4+).

Purpose: This is the highest-risk MEDIUM-confidence assumption for the whole M1 build. RESEARCH.md already RESOLVED the unknown by source inspection of `fastenhealth/fasten-onprem@main` (the ingest API exists: `POST /api/secure/source/manual` + `POST /api/secure/access/token`). This spike's job collapses from "discover an unknown" to "confirm a now-known contract empirically and record it" — making the 2-day timebox comfortably achievable. Plan-lock for phases 1.4/1.6/1.7/1.8 is blocked until this resolves.

Output: `docs/fasten-admin.md` (THE durable deliverable, committed to repo). The `spike/` compose + script + sample bundle are throwaway/reference artifacts. If blocked, an escalation note inside `docs/fasten-admin.md` documenting the blocker + chosen fallback rank is itself a valid completion (success criterion 5 — no silent overrun).

Timebox: 2 working days. This is a spike, not a production build. NO production Traefik/Vaultwarden/healthcheck wiring (that is Phase 1.4). Throwaway `FASTEN_JWT_ISSUER_KEY` + throwaway admin password only.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/HEALTH-01.0-fasten-ingest-api-spike/01.0-RESEARCH.md
@.planning/phases/HEALTH-01.0-fasten-ingest-api-spike/01.0-VALIDATION.md
@CLAUDE.md

<interfaces>
<!-- Verified ingest contract from RESEARCH.md (read directly from fasten-onprem@main source via gh api). -->
<!-- Executor uses these directly — no codebase or upstream exploration needed; RESEARCH.md is authoritative. -->

Auth flow (two-step — RESEARCH.md Pattern 1):
- POST /api/auth/signin   body {"username": <admin>, "password": <admin>}
    -> 200 {"success":true,"data":"<session JWT, 1h expiry>"}
- POST /api/secure/access/token   header Authorization: Bearer <session JWT>
    body {"name":"etl-spike","expiration":0}   (0 => exp 2099-12-31, long-lived)
    -> 200 {"success":true,"data":"<access JWT, long-lived>"}

Ingest (primary path — RESEARCH.md Pattern 2):
- POST /api/secure/source/manual   header Authorization: Bearer <access JWT>
    Content-Type: multipart/form-data, single form field  file=("bundle.json", bytes, "application/json")
    -> 200 {"success":true,"data":<sync summary>,"source":<manualSourceCredential>}
    -> 500 {"success":false,"error":"<err.Error() — echoes parse failure>"}   (only 200 or 500; no 4xx for body errors)

Verify:
- GET /api/secure/resource/fhir   header Authorization: Bearer <access JWT>
    -> ingested resource(s) present (canary string retrievable)

Container defaults (RESEARCH.md Code Examples — [VERIFIED: gh api main config.go]):
- web.listen.port = 8080
- database.location = /opt/fasten/db/fasten.db   (named volume mount target)
- log.level default = INFO   (settable to WARN)
- jwt.issuer.key INSECURE DEFAULT = literal "thisismysupersecuressessionsecretlength"  (MUST override)
- First signup (userCount == 0) becomes admin role automatically
- Env-var override form to try first: FASTEN_JWT_ISSUER_KEY / FASTEN_LOG_LEVEL (viper); fallback = bind-mounted config.yaml (jwt.issuer.key / log.level) — Open Question 2

Pinned image (INFRA-06 — multi-arch OCI INDEX digest, NOT per-arch):
  ghcr.io/fastenhealth/fasten-onprem:main@sha256:3f0192ac77dda7fd0e25175f7383a426f6d5ee11be7981b8753c10f6b7447d91

Open Questions the spike resolves in-line (RESEARCH.md, 30-min iterations, inside the box):
- OQ1/A2: does manual-source accept an arbitrary FHIR R4 collection Bundle, or a Fasten-export-profiled one? Build minimal Bundle{type:"collection", entry:[Observation]} via fhir.resources; if 500, read the echoed error string and adjust Bundle shape (same endpoint).
- OQ2/A1: exact env-var key names — try FASTEN_* first, fallback to mounted config.yaml.
- A3 (Pitfall 4): re-POST identical Bundle is expected non-idempotent (creates duplicates) — measure directly, informs Phase 1.4 DATA-09 dedup design.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wave 0 — scaffold throwaway pinned Fasten + assertive spike harness + synthetic canary Bundle</name>
  <read_first>
    - .planning/phases/HEALTH-01.0-fasten-ingest-api-spike/01.0-RESEARCH.md (Architecture Patterns 1+2, Code Examples, Recommended Spike Workspace Structure, Pitfall 1 jwt.issuer.key, Open Questions 1+2)
    - .planning/phases/HEALTH-01.0-fasten-ingest-api-spike/01.0-VALIDATION.md (Wave 0 Requirements, Per-Task Verification Map)
    - CLAUDE.md (PII Tier 1 strict: synthetic sentinel only; GPL-3.0 firewall: HTTP-only, no Fasten source patching; throwaway secrets only — never production Vaultwarden values)
  </read_first>
  <files>
    .planning/phases/HEALTH-01.0-fasten-ingest-api-spike/spike/compose.spike.yaml,
    .planning/phases/HEALTH-01.0-fasten-ingest-api-spike/spike/sample_bundle.json,
    .planning/phases/HEALTH-01.0-fasten-ingest-api-spike/spike/spike_ingest.py
  </files>
  <action>
Create the three Wave 0 throwaway artifacts (VALIDATION.md mandates these exist before the spike can run; all three are currently MISSING).

1. `compose.spike.yaml`: single service `fasten`, container_name `health-fasten-spike`, image pinned to the multi-arch OCI INDEX digest `ghcr.io/fastenhealth/fasten-onprem:main@sha256:3f0192ac77dda7fd0e25175f7383a426f6d5ee11be7981b8753c10f6b7447d91` (NOT the per-arch `linux/amd64` digest — INFRA-06 cross-host parity). Map port `8080:8080`. Set env `FASTEN_JWT_ISSUER_KEY=spike-throwaway-key-not-prod` (overrides the published insecure default `thisismysupersecuressessionsecretlength` — Pitfall 1; this is a THROWAWAY value, never the production Vaultwarden secret) and `FASTEN_LOG_LEVEL=INFO`. Named volume `fasten-spike-db:/opt/fasten/db`. No Traefik, no Vaultwarden, no healthcheck (out of 1.0 scope — that is Phase 1.4).

2. `sample_bundle.json`: a FHIR R4 `Bundle{type:"collection"}` with exactly one `entry` — one `Observation` with `subject` referencing a synthetic `Patient`. The ONLY "PII"-shaped values are the exact sentinel string `ZZZ-PII-CANARY-0001` placed in the Patient name AND in an Observation note/free-text field, so the later `docker logs` grep is unambiguous. Per CLAUDE.md PII Tier 1: every value is obviously synthetic; never invent a plausible real health value (no real lab numbers, no real names). This file is generated/validated via `fhir.resources` pydantic models in the script — keep the static JSON minimal and synthetic.

3. `spike_ingest.py`: assertive Python script (host Python 3.12, `pip install "httpx>=0.27" "fhir.resources>=8.0.0"` at run — spike-local, no production manifest change). Structure per RESEARCH.md Patterns 1+2:
   - Read `BASE=http://localhost:8080`, admin user/password from env or hardcoded throwaway constants (throwaway only — never the production secret).
   - Step A: `POST /api/auth/signin` -> assert `success==true`, capture `data` as `session_jwt`.
   - Step B: `POST /api/secure/access/token` with `Authorization: Bearer <session_jwt>`, body `{"name":"etl-spike","expiration":0}` -> assert `success==true`, capture `data` as `access_jwt`.
   - Step C: build a minimal `Bundle{type:"collection", entry:[Observation(subject=Patient/<id>)]}` via `fhir.resources` pydantic models, embedding the `ZZZ-PII-CANARY-0001` sentinel; validate (preflight gate); serialize to bytes.
   - Step D: `POST /api/secure/source/manual` with `Authorization: Bearer <access_jwt>`, multipart form field `file=("bundle.json", bundle_bytes, "application/json")`. On 500, print the echoed `error` string and `assert` with a clear message so OQ1 (Bundle profile tolerance) can be resolved by a 30-min Bundle-shape iteration.
   - Step E: `GET /api/secure/resource/fhir` with the access token -> `assert` the `ZZZ-PII-CANARY-0001` sentinel is present in the response (resource visible within seconds — success criterion 1).
   - Step F (gated by `--twice` flag): re-POST the identical Bundle, GET again, print the resource-count delta (RESEARCH.md A3/Pitfall 4 — expected non-idempotent; informs Phase 1.4 DATA-09 dedup design; do NOT assert a specific count, just record the delta).
   - Step G: a `--pii-grep` helper mode (or printed instructions) that the operator runs as `docker logs health-fasten-spike 2>&1 | Select-String "ZZZ-PII-CANARY"`.
   - Exit codes: `0` = all of steps A–E asserted clean (success criteria 1 met); non-zero = a specific assertion failed (drives the success-criterion-5 escalation path in Task 3). Print which success criterion failed.

GPL-3.0 firewall (COMPL-06): the script talks to Fasten over HTTP ONLY. Do NOT patch, fork, or shared-link any Fasten source. The spike may read upstream source for understanding (already done in RESEARCH.md) but produces no fork.
  </action>
  <verify>
    <automated>python -c "import ast,sys; ast.parse(open(r'.planning/phases/HEALTH-01.0-fasten-ingest-api-spike/spike/spike_ingest.py').read()); import json; json.load(open(r'.planning/phases/HEALTH-01.0-fasten-ingest-api-spike/spike/sample_bundle.json')); sys.exit(0)"</automated>
  </verify>
  <acceptance_criteria>
    - `spike/compose.spike.yaml` exists and contains the exact string `sha256:3f0192ac77dda7fd0e25175f7383a426f6d5ee11be7981b8753c10f6b7447d91` and `FASTEN_JWT_ISSUER_KEY` (override present, value is the throwaway, not `thisismysupersecuressessionsecretlength`).
    - `spike/sample_bundle.json` is valid JSON, is a FHIR Bundle with `type` `collection`, contains exactly one Observation entry, and contains the exact sentinel `ZZZ-PII-CANARY-0001` (no real health values, no plausible-real names/numbers — CLAUDE.md PII Tier 1).
    - `spike/spike_ingest.py` parses as valid Python, references `/api/auth/signin`, `/api/secure/access/token`, `/api/secure/source/manual`, and `/api/secure/resource/fhir`, uses multipart form field name `file`, and exits non-zero on any assertion failure.
    - No Fasten source file is created/patched anywhere in the repo (GPL-3.0 firewall — grep confirms no `fasten-onprem` source tree was vendored).
  </acceptance_criteria>
  <done>The three throwaway Wave 0 artifacts exist, parse/validate statically, embed the pinned index digest + jwt.issuer.key override + the `ZZZ-PII-CANARY-0001` sentinel, and the script targets the four verified endpoints over HTTP only with non-zero exit on failure.</done>
</task>

<task type="auto">
  <name>Task 2: Run the spike — bring up Fasten, confirm ingest contract, run idempotency probe + PII-log audit</name>
  <read_first>
    - .planning/phases/HEALTH-01.0-fasten-ingest-api-spike/spike/compose.spike.yaml (just created — the image pin + env)
    - .planning/phases/HEALTH-01.0-fasten-ingest-api-spike/spike/spike_ingest.py (just created — the assertions)
    - .planning/phases/HEALTH-01.0-fasten-ingest-api-spike/01.0-RESEARCH.md (Pitfall 2 PII-log audit method, Pitfall 4 non-idempotency probe, Pin verification, Open Questions 1+2 in-line resolution)
    - .planning/phases/HEALTH-01.0-fasten-ingest-api-spike/01.0-VALIDATION.md (Manual-Only Verifications: PII-log audit + multi-arch digest pin correctness)
    - CLAUDE.md (PII Tier 1: docker logs excerpts must contain no real medical data before any commit — only the synthetic sentinel)
  </read_first>
  <files>
    .planning/phases/HEALTH-01.0-fasten-ingest-api-spike/spike/spike_ingest.py
  </files>
  <action>
Empirically run the now-known contract (RESEARCH.md confirmed it exists; this confirms it behaves as documented). Adjust ONLY `spike_ingest.py` if an in-line Open Question needs resolution (Bundle shape per OQ1, env-var key form per OQ2 — 30-min iterations, well inside the 2-day box).

1. Pull + boot: `docker compose -f .planning/phases/HEALTH-01.0-fasten-ingest-api-spike/spike/compose.spike.yaml up -d`. Wait for `health-fasten-spike` to be ready on `:8080`.
2. Provision the admin: complete the first-signup wizard (first user with `userCount==0` becomes `admin` — RESEARCH.md). Use a THROWAWAY username/password (never the production Vaultwarden secret).
3. Run `python .planning/phases/HEALTH-01.0-fasten-ingest-api-spike/spike/spike_ingest.py` — it must signin, mint the access token, build+validate the Bundle, POST `/api/secure/source/manual`, and assert the `ZZZ-PII-CANARY-0001` sentinel is retrievable via `GET /api/secure/resource/fhir` within seconds (exit 0 = success criterion 1 met).
   - If POST returns 500: read the echoed `error` string, adjust the Bundle shape in `sample_bundle.json`/script per OQ1/A2, re-run. Same endpoint — this is a Bundle-profile iteration, not a fallback.
   - If `FASTEN_JWT_ISSUER_KEY`/`FASTEN_LOG_LEVEL` env override does not take effect (OQ2/A1): bind-mount a `config.yaml` (`jwt.issuer.key`, `log.level`) in `compose.spike.yaml` instead; record the working form for Task 3.
4. Idempotency probe (Pitfall 4 / A3): run `python ... spike_ingest.py --twice`; record the resource-count delta (expected: duplicates created — Fasten manual-source is NOT idempotent). This is a measurement, not an assertion; the finding informs Phase 1.4 DATA-09 dedup design.
5. Multi-arch digest confirmation (INFRA-06): run `docker buildx imagetools inspect ghcr.io/fastenhealth/fasten-onprem:main`; capture the OCI **index** digest and confirm it equals `sha256:3f0192ac77dda7fd0e25175f7383a426f6d5ee11be7981b8753c10f6b7447d91` AND differs from the per-arch `linux/amd64` digest. (If upstream `:main` has been rebuilt and the index digest changed, record BOTH the pinned and the now-current digest for the Task 3 escalation/note — do not silently re-pin.)
6. PII-log audit (DATA-02 paired deliverable — the core security finding): with `LOG_LEVEL=INFO`, run `docker logs health-fasten-spike 2>&1 | Select-String "ZZZ-PII-CANARY"`. Record whether the sentinel surfaces. Then restart with `FASTEN_LOG_LEVEL=WARN` (or `config.yaml log.level: warn`), re-ingest, repeat the grep. Record findings for both levels. Per CLAUDE.md: any `docker logs` excerpt captured for the doc must contain ONLY the synthetic sentinel — never a real medical value (there are none, by construction).

Collect all raw findings (endpoint behaviour, response envelopes seen, 200/500 bodies with the sentinel only, idempotency delta, both digests, canary-at-INFO vs canary-at-WARN) for Task 3 to write into `docs/fasten-admin.md`. Tear-down is deferred to Task 3 (the doc may need a final re-check).
  </action>
  <verify>
    <automated>docker compose -f .planning/phases/HEALTH-01.0-fasten-ingest-api-spike/spike/compose.spike.yaml up -d && python .planning/phases/HEALTH-01.0-fasten-ingest-api-spike/spike/spike_ingest.py; echo "EXIT=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `spike_ingest.py` exits `0` against the live `health-fasten-spike` container (signin + access-token mint + manual-source POST + `GET /api/secure/resource/fhir` all asserted clean — success criterion 1 empirically TRUE) — OR a specific non-zero exit + the failing assertion message is captured for the Task 3 escalation path (success criterion 5).
    - The `ZZZ-PII-CANARY-0001` sentinel is retrievable in the `GET /api/secure/resource/fhir` response within seconds of the POST (resource visible — success criterion 1).
    - `spike_ingest.py --twice` resource-count delta is recorded (Pitfall 4 / DATA-09 input for Phase 1.4).
    - `docker buildx imagetools inspect` output captured; the OCI index digest is confirmed (matches the pin, or any drift is recorded for Task 3 — no silent re-pin).
    - PII-log grep result recorded for BOTH `LOG_LEVEL=INFO` and `LOG_LEVEL=WARN` (canary present/absent at each level); captured excerpts contain only the synthetic sentinel (CLAUDE.md PII Tier 1).
  </acceptance_criteria>
  <done>The contract is empirically confirmed (script exits 0) or its blocker is captured with a specific failing assertion; the idempotency delta, the confirmed/observed digest, and the INFO-vs-WARN PII-log findings are all recorded and ready to write into the durable doc.</done>
</task>

<task type="auto">
  <name>Task 3: Write docs/fasten-admin.md (durable deliverable) + escalation fallback path, commit, tear down</name>
  <read_first>
    - .planning/phases/HEALTH-01.0-fasten-ingest-api-spike/01.0-RESEARCH.md (Patterns 1+2, response semantics, Fallback Path Ranking table, Security Domain threat patterns, Pitfall 1 jwt.issuer.key note)
    - .planning/phases/HEALTH-01.0-fasten-ingest-api-spike/01.0-VALIDATION.md (Validation Sign-Off, Manual-Only Verifications — what the doc must record)
    - .planning/ROADMAP.md (Phase 1.0 Success Criteria 1–5 — the doc must satisfy all five, including the escalation-note fallback for criterion 5)
    - CLAUDE.md (data isolation: docs/fasten-admin.md documents the CONTRACT, never sample patient data; Vault write rule — product-knowledge eligible, never real health values)
  </read_first>
  <files>
    docs/fasten-admin.md
  </files>
  <action>
Write `docs/fasten-admin.md` — THE durable deliverable, the source of truth for downstream ETL phases 1.4+ (the `spike/` artifacts are throwaway/reference; this file is what survives). Structure it to satisfy all five ROADMAP Phase 1.0 success criteria:

1. **Ingest endpoint contract** (success criteria 1, 3): document `POST /api/secure/source/manual` — multipart `form-data`, single field `file` holding a FHIR Bundle JSON; the two-step auth (`POST /api/auth/signin` -> 1h session JWT -> `POST /api/secure/access/token` with `{"name":...,"expiration":0}` -> long-lived `access` JWT); the response envelope (`200 {"success":true,"data":<summary>,"source":<manualSourceCredential>}`; `500 {"success":false,"error":...}`; only 200/500, no 4xx for body errors); the verify path (`GET /api/secure/resource/fhir`). Record the empirically observed Bundle profile tolerance from Task 2 OQ1 (which Bundle.type/shape was accepted) and the working env-var/config form from OQ2.
2. **Pinned digest** (success criterion 3): record `ghcr.io/fastenhealth/fasten-onprem:main@sha256:3f0192ac77dda7fd0e25175f7383a426f6d5ee11be7981b8753c10f6b7447d91` as the multi-arch OCI INDEX digest (state explicitly: pin the index, NOT the per-arch digest — INFRA-06 cross-host parity). Paste the Task 2 `docker buildx imagetools inspect` index-vs-per-arch evidence. Note Phase 1.11 owns the 8-week re-pin cadence (INFRA-09).
3. **Security note** (jwt.issuer.key — Pitfall 1): document that Fasten's `jwt.issuer.key` defaults to the published constant `thisismysupersecuressessionsecretlength`; with it unchanged anyone can forge an admin JWT. Flag as a MANDATORY Phase 1.1/1.4 Vaultwarden-sourced secret (`FASTEN_JWT_ISSUER_KEY`, `bw get default/<secret>`). Note the long-lived `access` token must be stored via `bw get`, never in repo/image/env-inspectable.
4. **LOG_LEVEL decision** (success criterion 2 — paired PII deliverable): record the Task 2 audit findings (canary present/absent at INFO vs WARN) and the chosen mitigation: `FASTEN_LOG_LEVEL=WARN` + Docker `local` driver rotation (OPS-07 `max-size:10m max-file:3`) + restricted host log mount + optional redaction sidecar (vector/fluent-bit) ONLY if WARN still leaks. Record the idempotency finding (manual-source is NOT idempotent — Phase 1.4 must enforce `meta.tag.code = sha256(canonical_payload)` dedup, DATA-09).
5. **Fallback Path Ranking** (success criterion 1 contingency): copy the RESEARCH.md ranked table (rank 0 PRIMARY `/api/secure/source/manual`+access token; rank 1 `resource/composition`; rank 2 volume-drop demoted; rank 3 direct SQLite; rank 4 UI scrape) so a future upstream breaking change has a pre-analysed contingency.
6. **Escalation section** (success criterion 5 — the no-silent-overrun fallback): include a clearly headed "## Escalation / Blocked Outcome" section. If Task 2 confirmed the primary path (`spike_ingest.py` exited 0), state PRIMARY PATH CONFIRMED with the evidence. If Task 2 was BLOCKED (non-zero exit / unrecoverable failure / network egress blocked / digest drift unresolvable), this section instead documents: the exact blocker, the failing assertion, the chosen fallback rank from the table above, and the concrete next-step commands for Phase 1.4. Either outcome makes this doc a valid phase completion — no silent overrun.

Per CLAUDE.md data-isolation: this file documents the CONTRACT only — no sample patient data, only the synthetic sentinel may appear in any illustrative excerpt.

Then: commit `docs/fasten-admin.md` (durable) + the `spike/` artifacts (reference) with a Conventional Commit message and the `Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>` trailer. Finally tear down the throwaway container + volume: `docker compose -f .planning/phases/HEALTH-01.0-fasten-ingest-api-spike/spike/compose.spike.yaml down -v` (destroys the synthetic-only SQLite — no real PII was ever ingested, by construction; retained image layers are intentional cache for Phase 1.4).
  </action>
  <verify>
    <automated>python -c "import sys; d=open(r'docs/fasten-admin.md',encoding='utf-8').read(); req=['/api/secure/source/manual','/api/secure/access/token','sha256:3f0192ac77dda7fd0e25175f7383a426f6d5ee11be7981b8753c10f6b7447d91','jwt.issuer.key','LOG_LEVEL','Escalation']; missing=[t for t in req if t not in d]; print('MISSING',missing) or sys.exit(1) if missing else sys.exit(0)"</automated>
  </verify>
  <acceptance_criteria>
    - `docs/fasten-admin.md` exists and contains ALL of: `/api/secure/source/manual`, `/api/secure/access/token`, the exact digest `sha256:3f0192ac77dda7fd0e25175f7383a426f6d5ee11be7981b8753c10f6b7447d91`, `jwt.issuer.key`, `LOG_LEVEL`, and a heading containing `Escalation`.
    - The Fallback Path Ranking table (rank 0 PRIMARY through rank 4) is present.
    - The "## Escalation / Blocked Outcome" section states either PRIMARY PATH CONFIRMED (with evidence) OR documents the blocker + chosen fallback rank + Phase 1.4 next-step commands (success criterion 5 — no silent overrun).
    - The PII-log decision records canary findings at INFO vs WARN and the chosen mitigation; the idempotency finding (non-idempotent manual-source -> DATA-09) is recorded.
    - No real or plausible-real health data appears anywhere in the file — only the synthetic `ZZZ-PII-CANARY-0001` sentinel may appear in an excerpt (CLAUDE.md data isolation).
    - `docs/fasten-admin.md` is committed to git; the throwaway container + named volume are torn down (`docker compose ... down -v`).
  </acceptance_criteria>
  <done>`docs/fasten-admin.md` is committed as the source of truth for Phase 1.4+, satisfying ROADMAP success criteria 1–5 (including the escalation-note fallback for criterion 5); the throwaway container + volume are destroyed and no real PII was ever ingested.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| spike script → Fasten API (localhost:8080) | Authenticated HTTP; bearer JWT crosses here |
| Fasten container → Docker engine stdout (json-file/local driver) | Bundle-derived content (incl. canary) can cross into host-persisted logs — outside the project's pino redaction allowlist |
| upstream `:main` tag → pinned digest | Supply-chain boundary; floating tag vs frozen digest |
| synthetic spike data → committed repo artifacts | PII Tier 1 boundary: only synthetic sentinel may cross into git |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1.0-01 | Spoofing / Elevation | Fasten HS256 JWT signed with published default `jwt.issuer.key` | mitigate | Task 1 sets throwaway `FASTEN_JWT_ISSUER_KEY` overriding `thisismysupersecuressessionsecretlength`; Task 3 documents it as a MANDATORY Vaultwarden-sourced secret for Phase 1.1/1.4 (acceptance-criterion enforced) |
| T-1.0-02 | Tampering | Floating `:main` supply-chain drift | mitigate | Task 1 pins the multi-arch OCI INDEX digest `sha256:3f0192ac...`; Task 2 confirms via `docker buildx imagetools inspect`; any drift recorded (no silent re-pin); Phase 1.11 owns 8-week re-pin (INFRA-09) + Phase 1.1 SEC-08 license/image-hash pre-commit |
| T-1.0-03 | Information Disclosure | PII Tier 1 in `docker logs` / host json-file (Fasten logrus + `err.Error()` echo) | mitigate | Task 2 empirical canary audit at INFO and WARN; Task 3 records `LOG_LEVEL=WARN` + `local` driver rotation (OPS-07) + restricted mount + optional redaction sidecar decision. **High severity — paired core deliverable, blocked until audited & documented.** |
| T-1.0-04 | Spoofing | Long-lived `access` token leakage (repo/image/env) | mitigate | Spike uses throwaway values only; Task 3 documents `access` token MUST be stored via `bw get`, never in repo/image, rotated on 8-week re-pin (INFRA-09) |
| T-1.0-05 | Information Disclosure | Synthetic-vs-real data confusion in spike artifacts | mitigate | CLAUDE.md PII Tier 1 enforced as acceptance criteria on every task: `sample_bundle.json`, `docker logs` excerpts, and `docs/fasten-admin.md` contain ONLY the `ZZZ-PII-CANARY-0001` sentinel — never real or plausible-real health values |
| T-1.0-06 | Tampering / License | GPL-3.0 firewall breach via patching Fasten source | accept→mitigate | HTTP-only enforced; Task 1 acceptance criterion greps to confirm no Fasten source tree vendored/patched (COMPL-06). Rationale: spike reads upstream source for understanding only (already in RESEARCH.md), produces no fork |

All high-severity threats (T-1.0-03 PII-in-logs) are mitigated via blocking acceptance criteria. No `[ASSUMED]`/`[SUS]`/`[SLOP]` packages (Package Legitimacy Audit in RESEARCH.md: no production manifest change ships; `httpx`/`fhir.resources` are spike-local and already in the locked ETL stack; Fasten container digest-pinned + Phase 1.1 SEC-08 gated) — no T-1.0-SC checkpoint required for this phase.
</threat_model>

<verification>
- `spike_ingest.py` exits `0` against the live throwaway Fasten container (signin → access-token mint → `POST /api/secure/source/manual` → `GET /api/secure/resource/fhir` canary assertion) — OR a specific non-zero exit drives the documented escalation path.
- `docker buildx imagetools inspect` confirms the pinned OCI index digest (or records drift).
- PII-log canary audit completed at `LOG_LEVEL=INFO` and `WARN`; mitigation recorded.
- `docs/fasten-admin.md` contains the endpoint contract, auth pattern, response semantics, pinned digest, `jwt.issuer.key` security note, LOG_LEVEL decision, fallback ranking, and an Escalation/Blocked-Outcome section.
- No real or plausible-real health data in any committed artifact — only the synthetic `ZZZ-PII-CANARY-0001` sentinel.
- Throwaway container + named volume torn down (`docker compose ... down -v`).
</verification>

<success_criteria>
ROADMAP Phase 1.0 success criteria (the contract — no CONTEXT.md for this investigation spike):
1. A Python script POSTs a 1-resource FHIR Bundle to a running Fasten container and the resource appears (retrievable via API) within seconds — OR a documented fallback path with concrete commands.
2. Fasten stdout logs reviewed against PII Tier 1 baseline; findings noted with mitigation (LOG_LEVEL / redaction sidecar / restricted mount).
3. Chosen Fasten image digest recorded with the ingest endpoint, auth pattern, and response semantics.
4. `docs/fasten-admin.md` checked into the repo as source of truth for downstream ETL phases.
5. Spike completes within 2 working days; if blocked, an escalation note documents the blocker + chosen fallback (no silent overrun).
</success_criteria>

<output>
Create `.planning/phases/HEALTH-01.0-fasten-ingest-api-spike/01.0-01-SUMMARY.md` when done.
</output>
